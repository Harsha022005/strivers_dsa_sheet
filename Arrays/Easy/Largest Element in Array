
from typing import List


class Solution:
    def largest(self, n : int, arr : List[int]) -> int:
        # code here
        maximum=float('-inf')
        for i in range(n):
            if arr[i]>maximum:
                maximum=arr[i]
        return maximum        
